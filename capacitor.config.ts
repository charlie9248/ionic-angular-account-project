import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angualar-ionic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
